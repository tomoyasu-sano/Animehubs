// TODO: Cloudflare D1移行後、これらのテストはwrangler環境で再実装が必要
// better-sqlite3を使用していた旧テストはD1非互換のためスキップ
import { describe, it } from "vitest";

describe.skip("reservation-queries (D1 migration pending)", () => {
  it("placeholder", () => {});
});
